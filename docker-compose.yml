version: '3'
services:
  users-app:
    image: chlav/havryliuk-test-clear-solution:latest
    ports:
      - 8080:8080
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: users
    ports:
      - 5432:5432
